# START HERE

이 문서는 새로운 대화에서 코드 에이전트에게 처음 전달할 내용을 빠르게 찾기 위한 입구 문서입니다.

긴 작업이나 새 브랜치 작업은 이전 대화의 context를 계속 가져가기보다, 이 문서의 프롬프트를 복사해서 새 대화로 시작하는 것을 권장합니다.

---

## 0. 빠른 사용법

새 대화를 시작할 때는 아래 순서만 따르면 됩니다.

```text
1. 이 문서에서 프롬프트 하나를 고른다.
2. 새 대화에 복사한다.
3. "이번 작업 방향"만 내 작업에 맞게 바꾼다.
4. 문서만 할지, 코드까지 할지, 테스트까지 할지 한 줄로 제한한다.
5. 에이전트에게 진행시킨다.
```

가장 자주 쓰는 형태는 아래입니다.

```text
이번 작업 방향:
- 예약 기능 백엔드 추가

추가 제한:
- 이번 요청에서는 코드 구현은 하지 말고 문서 작성까지만 진행
```

또는 코드까지 진행할 때는 이렇게 씁니다.

```text
이번 작업 방향:
- 예약 가능 시간 조회 API 구현

추가 제한:
- 코드 구현까지 진행
- 빌드 확인까지 진행
- 서버 기동은 VS Code Spring Boot Dashboard로, DB는 Docker로, API 런타임 검증은 Swagger 대표 예시 1개로 내가 직접 확인할 수 있게 안내
- 실제 커밋, push는 하지 말 것
```

아래 선택지는 그대로 두지 말고 반드시 바꿔서 사용합니다.

```text
- [백엔드/프론트엔드/API 연결/배포/운영/문서 중 선택]
```

예시:

```text
- 백엔드 예약 기능 구현
- 프론트엔드 예약 화면 구현
- 프론트엔드와 백엔드 예약 API 연결
- Docker Compose 실행 문서 정리
- 브랜치 종료 전 PR 문서 작성
```

어떤 프롬프트를 써야 할지 헷갈리면 아래 기준으로 고릅니다.

| 상황                                    | 사용할 프롬프트                                                             |
| --------------------------------------- | --------------------------------------------------------------------------- |
| 다음 작업을 에이전트가 고르게 하고 싶다 | `2. 가장 기본 시작 프롬프트`                                                |
| 새 브랜치에서 기능을 진행한다           | `3. 새 브랜치 작업 시작 프롬프트`                                           |
| 문서만 만들고 싶다                      | 브랜치 프롬프트 + `코드 구현은 하지 말고 문서만 작성`                       |
| 코드까지 구현하고 싶다                  | 브랜치 프롬프트 + `코드 구현과 빌드 확인까지 진행, 실행 검증은 명령만 안내` |
| PR 문서가 필요하다                      | 브랜치 프롬프트 + `PR 문서 초안 작성`                                       |

---

## 1. 언제 이 문서를 사용하나요?

| 상황                              | 추천                      |
| --------------------------------- | ------------------------- |
| 짧은 수정, 오탈자, 작은 문서 보강 | 기존 대화에서 이어서 진행 |
| 새 기능 구현                      | 새 대화 시작 권장         |
| 새 브랜치 작업                    | 새 대화 시작 권장         |
| 백엔드/프론트엔드 큰 기능 구현    | 새 대화 시작 권장         |
| 배포/운영 정리                    | 새 대화 시작 권장         |
| PR 문서 작성과 브랜치 종료 정리   | 새 대화 시작 권장         |

이유:

- 긴 대화는 context 한계 때문에 이전 결정이나 문서를 일부 놓칠 수 있습니다.
- 새 대화에서 `docs/README.md`부터 다시 읽게 하면 프로젝트 기준을 다시 고정할 수 있습니다.
- 브랜치 단위로 대화를 나누면 구현 기록과 PR 문서 작성이 깔끔해집니다.

---

## 2. 가장 기본 시작 프롬프트

아직 브랜치를 만들지 않았거나, 다음 작업을 에이전트가 고르게 하고 싶을 때 사용합니다.

```text
docs/README.md를 읽고,
docs/09_agent/05_문서기반_자동진행_운영가이드.md 기준으로
현재 프로젝트 상태를 확인한 뒤 다음 작업을 하나 진행해줘.

작업 후:
- 관련 체크리스트 갱신
- 확인한 것과 확인하지 못한 것 구분
- 다음 작업 후보 1~3개 제안
```

---

## 3. 새 브랜치 작업 시작 프롬프트

새 기능을 구현하거나 긴 작업을 시작할 때 사용합니다.

```text
docs/README.md를 읽고,
docs/09_agent/05_문서기반_자동진행_운영가이드.md와
docs/11_implementation_log/00_브랜치_작업_기록_가이드.md 기준으로
이번 브랜치 작업을 진행해줘.

이번 작업 방향:
- [백엔드/프론트엔드/API 연결/배포/운영/문서 중 선택]

요청:
1. docs/13_schedule/02_전체_작업_체크리스트.md에서 관련 항목 확인
2. 이번 브랜치에서 할 작업 범위 제안
3. 브랜치별 체크리스트 초안 작성
4. 관련 문서와 현재 코드 상태 확인
5. 구현 또는 문서 수정 진행
6. 작업 후 브랜치 기록 갱신
7. 전체 체크리스트 갱신
8. PR 문서 초안 작성

주의:
- 작업은 한 번에 하나의 작은 단위로 진행
- 전자정부프레임워크 Simple Backend Template 구조 유지
- Maven 유지
- MyBatis 기준 유지
- 신규 백엔드 도메인은 egovframework.healthcenter 하위에 작성
- 에이전트는 일반 명령, 정적 확인, Maven 빌드/테스트 컴파일까지만 직접 실행
- 서버 기동, Docker 실행, API 런타임 호출, Swagger/브라우저 확인, 포트 점유 프로세스 종료는 사용자가 직접 수행
- API 런타임 검증 안내는 Swagger `Try it out`을 우선하고, 요청 예시는 대표 예시 1개만 작성
- Swagger 대표 예시가 바로 동작하도록 기능 구현 시 seed/mock 데이터 필요 여부를 확인하고, 필요하면 목적별 고정 예시 데이터를 추가
- 추가 Happy/Edge/Bad 케이스는 PR 문서 체크리스트로 정리
- 대화 마지막에 수정한 내용을 반영한 커밋 제목과 내용을 한글로 줄 것
- 실제 커밋, push, 배포는 내가 명시하기 전까지 하지 말 것
```

---

## 4. 방향별 한 줄 추가 문장

위 프롬프트의 `이번 작업 방향` 아래에 필요한 문장을 붙여 사용합니다.

### 백엔드

```text
이번에는 백엔드 기능 구현 방향으로 진행해줘.
전자정부프레임워크 구조, Maven, MyBatis, 공통 응답 형식을 유지해줘.
```

### 프론트엔드

```text
이번에는 프론트엔드 화면 구현 또는 API 연결 방향으로 진행해줘.
화면 설계서와 UX/API 계약 문서를 먼저 확인해줘.
```

### API 연결

```text
이번에는 프론트엔드와 백엔드 API 연결 방향으로 진행해줘.
API 명세서와 구현된 API 응답 형식을 비교해줘.
```

### 배포

```text
이번에는 Docker Compose 기반 배포와 실행 환경 정리 방향으로 진행해줘.
실행 명령, 포트, 환경변수, 확인 URL을 문서화해줘.
```

### 운영

```text
이번에는 운영 준비와 장애 대응 정리 방향으로 진행해줘.
로그, 예외 응답, 민감 정보, 운영 체크리스트를 확인해줘.
```

### 문서/PR

```text
이번에는 브랜치 종료 전 문서 정리와 PR 작성안 생성 방향으로 진행해줘.
구현 기록, 미검증 사유, 후속 작업을 명확히 정리해줘.
```

---

## 5. 에이전트가 반드시 먼저 읽을 문서

새 대화에서 에이전트는 최소한 아래 문서를 읽어야 합니다.

1. `docs/README.md`
2. `docs/09_agent/05_문서기반_자동진행_운영가이드.md`
3. `docs/09_agent/06_새대화_시작_가이드.md`
4. `docs/13_schedule/02_전체_작업_체크리스트.md`

브랜치 작업이면 추가로 읽습니다.

5. `docs/11_implementation_log/00_브랜치_작업_기록_가이드.md`

작업 성격에 따라 관련 설계 문서를 추가로 읽습니다.

| 작업       | 추가 문서                                                                                                       |
| ---------- | --------------------------------------------------------------------------------------------------------------- |
| 백엔드     | `docs/09_agent/01_코드_에이전트_작업_가이드.md`, `docs/09_agent/02_에이전트_프롬프트_및_코드작성_지침.md`       |
| DB/MyBatis | `docs/03_database/01_ERD_및_테이블_명세서.md`, `docs/10_backend_transition/04_DB_접근_방식_JPA_MyBatis_판단.md` |
| API        | `docs/04_api/01_API_명세서.md`                                                                                  |
| 프론트엔드 | `docs/05_frontend/01_화면_설계서_v0_프롬프트.md`, `docs/05_frontend/02_UX_API_계약_우선순위.md`                 |
| 일정       | `docs/13_schedule/01_남은_일정_단계별_진행_계획.md`                                                             |
| 포트폴리오 | `docs/12_portfolio/01_포트폴리오_구현_스토리라인.md`                                                            |

---

## 6. 이 프로젝트의 고정 기준

에이전트가 새 대화에서 흔들리면 안 되는 기준입니다.

| 항목               | 기준                                                   |
| ------------------ | ------------------------------------------------------ |
| 프로젝트           | 보건소 스마트 예약·대기 및 혼잡도 분석 시스템          |
| Backend            | eGovFrame Simple Backend Template 기반                 |
| Build Tool         | Maven                                                  |
| DB                 | PostgreSQL 18 + pgvector Docker 이미지                 |
| DB 접근            | MyBatis                                                |
| Frontend           | React                                                  |
| API 응답           | `success + data + error`                               |
| 신규 백엔드 패키지 | `egovframework.healthcenter`                           |
| Mapper XML         | `src/main/resources/egovframework/mapper/healthcenter` |
| 배포               | Docker Compose 기준                                    |

하지 않을 것:

- Spring Initializr로 백엔드 재생성
- Gradle 전환
- Maven과 Gradle 혼용
- MVP에서 JPA 신규 도입
- 전자정부프레임워크 핵심 설정 임의 삭제
- MVP에서 AI/pgvector 기능 구현
- 서버 기동, Docker 실행, API 런타임 호출, Swagger/브라우저 확인을 사용자 요청 없이 직접 수행
- 포트 점유 프로세스 종료, 방화벽/권한이 걸릴 수 있는 실행 작업을 사용자 요청 없이 직접 수행
- 실제 커밋, push, 배포를 사용자 승인 없이 수행

---

## 7. 작업 후 보고받을 내용

에이전트는 작업 후 아래 형식으로 보고하는 것이 좋습니다.

```text
작업 결과

1. 읽은 문서
2. 진행한 작업
3. 변경 파일
4. 확인 결과
   - 빌드:
   - 테스트:
   - 실행: 사용자가 직접 확인할 Docker/Spring Boot Dashboard 기준과 기대 결과
   - API: Swagger 대표 예시 1개, 필요한 seed/mock 데이터, PR 문서의 추가 테스트 체크리스트
5. 체크리스트 갱신
   - 전체 체크리스트:
   - 브랜치별 체크리스트:
6. 남은 위험
7. 다음 작업 추천
8. PR 문서 또는 커밋 메시지 초안
```

---

## 8. 핵심 요약

새 대화는 이 문서에서 시작합니다.

일반 작업은 기본 시작 프롬프트를 사용합니다.  
긴 작업이나 새 브랜치 작업은 새 브랜치 작업 시작 프롬프트를 사용합니다.

```text
짧은 작업 = 기존 대화에서 계속
긴 작업 = START_HERE.md 복사 후 새 대화
브랜치 작업 = START_HERE.md의 브랜치 프롬프트로 새 대화
```
