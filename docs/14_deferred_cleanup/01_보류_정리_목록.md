# 보류 정리 목록

## 1. 문서 목적

이 문서는 지금 바로 삭제하거나 바꾸기에는 애매하지만, 기능 개발이 어느 정도 끝난 뒤 반드시 다시 확인해야 할 항목을 모아두는 곳이다.

작업 중 아래처럼 판단되는 항목은 즉시 삭제하지 않고 이 문서로 보낸다.

- eGovFrame 공통 골격과 연결되어 있어 영향 범위가 큰 항목
- 지금은 사용하지 않는 것처럼 보이지만 다른 설정, 테스트, 보안 흐름과 연결될 수 있는 항목
- MVP에서는 필요 없지만 포트폴리오 설명 또는 향후 확장 후보로 남길 수 있는 항목
- 삭제해도 될 가능성이 크지만 별도 브랜치에서 더 좁게 검증하는 편이 안전한 항목

이 문서는 “하지 않을 일 목록”이 아니라 “나중에 깔끔하게 정리할 후보 목록”이다.

## 2. 기록 원칙

- 보류한 이유를 짧게 적는다.
- 지금 삭제하지 않은 이유를 적는다.
- 나중에 어떤 조건이 충족되면 정리할지 적는다.
- 관련 문서와 파일 경로를 남긴다.
- 기능 구현 브랜치와 정리 브랜치를 가능하면 분리한다.

## 3. 상태 값

| 상태 | 의미 |
|---|---|
| `보류` | 아직 판단하지 않고 남겨둔다. |
| `확인 필요` | 다음 정리 브랜치에서 참조와 영향 범위를 확인한다. |
| `제거 후보` | 제거 가능성이 높지만 별도 검증 후 삭제한다. |
| `유지 후보` | 계속 남길 가능성이 높지만 이유를 확정해야 한다. |
| `정리 완료` | 별도 브랜치에서 정리했고 결과 문서가 있다. |

## 4. 보류 정리 목록

| ID | 항목 | 현재 판단 | 지금 보류한 이유 | 나중에 확인할 조건 | 관련 파일/문서 |
|---|---|---|---|---|---|
| DC-001 | `JwtAuthenticationFilter`의 레거시 `LoginVO` fallback | 확인 필요 | 레거시 로그인 샘플은 제거했지만 eGovFrame 보안 골격과 테스트가 아직 `LoginVO`, `EgovJwtTokenUtil`을 참조한다. | 신규 Auth/Member API 런타임 확인 후, 기존 eGovFrame JWT 토큰을 더 이상 받을 필요가 없는지 확인한다. | `backend/src/main/java/egovframework/com/jwt/JwtAuthenticationFilter.java`, `docs/11_implementation_log/19_eGovFrame_레거시_인증_샘플_제거_기록.md` |
| DC-002 | `EgovJwtTokenUtil` | 확인 필요 | 레거시 `LoginVO` 기반 JWT 유틸이지만 eGovFrame 보안 골격과 테스트에 남아 있다. | `JwtAuthenticationFilter` fallback 제거 여부를 먼저 결정한 뒤 사용처가 사라졌는지 `rg`로 확인한다. | `backend/src/main/java/egovframework/com/jwt/EgovJwtTokenUtil.java`, `backend/src/test/java/egovframework/com/jwt/EgovJwtTokenUtilTest.java` |
| DC-003 | `LoginVO`와 eGovFrame 사용자 인증 유틸 | 유지 후보 | eGovFrame 공통 클래스와 보안 유틸에서 참조한다. 프로젝트 목적상 전자정부 기반 흔적을 모두 제거하면 안 된다. | 보건소 도메인 구현이 안정화된 뒤 실제로 필요한 eGovFrame 공통 유틸인지 구분한다. | `backend/src/main/java/egovframework/com/cmm/LoginVO.java`, `backend/src/main/java/egovframework/com/cmm/util/EgovUserDetailsHelper.java`, `backend/src/main/java/egovframework/com/cmm/interceptor/AuthenticInterceptor.java` |
| DC-004 | Redis 기반 대기번호 고속 채번 | 보류 | 현재는 PostgreSQL `queue_ticket_counters`와 유니크 인덱스로 정합성을 먼저 보장했다. Redis를 추가하면 성능은 좋아질 수 있지만 Docker Compose, 장애 fallback, DB와 Redis counter 보정 정책이 함께 필요하다. | 대기표 발급량이 늘어나 DB row lock 경합이 병목인지 확인하거나, 포트폴리오 고도화 단계에서 Redis `INCR` + PostgreSQL fallback + DB max 기준 counter 보정 정책을 설계한다. | `backend/src/main/resources/db/postgresql/schema.sql`, `backend/src/main/resources/egovframework/mapper/healthcenter/queue/QueueTicket_SQL_postgresql.xml`, `docker-compose.yml`, `docs/11_implementation_log/40_Queue_Ticket_Number_Concurrency_구현_기록.md` |
| DC-005 | 동시 요청 자동화 테스트 후보 정리 | 보류 | 대기번호 중복, 예약 정원 초과, 중복 예약, 체크인 중복 같은 경쟁 조건은 별도 테스트 브랜치에서 병렬 요청 또는 트랜잭션 테스트로 다루는 편이 안전하다. 현재 브랜치는 Dashboard/Congestion API 구현에 집중한다. | 백엔드 주요 흐름이 안정화된 뒤 예약/방문/대기 정책별 Happy/Edge/Bad 및 동시성 케이스를 테스트 후보 문서나 자동화 테스트로 정리한다. | `docs/13_schedule/02_전체_작업_체크리스트.md`, `docs/11_implementation_log/40_Queue_Ticket_Number_Concurrency_구현_기록.md`, `backend/src/main/java/egovframework/healthcenter/visit/application/VisitCommandService.java`, `backend/src/main/java/egovframework/healthcenter/reservation/application/ReservationCommandService.java` |

## 5. 나중에 정리할 때의 작업 순서

1. 이 문서에서 `확인 필요` 또는 `제거 후보` 항목을 하나 고른다.
2. 별도 브랜치를 만든다.
3. `rg`와 GitNexus가 가능하면 impact 분석으로 참조 범위를 확인한다.
4. 제거 대상과 유지 대상을 구현 기록에 먼저 적는다.
5. 한 기능 묶음만 제거한다.
6. `mvn -q -DskipTests compile`과 `mvn -q test-compile`을 확인한다.
7. 서버 기동, API 호출, Swagger 확인은 사용자가 직접 수행할 명령과 기대 결과를 문서화한다.
8. 정리 결과를 이 문서에 반영한다.

## 6. 기록 템플릿

새 보류 항목은 아래 형식으로 추가한다.

```markdown
| DC-번호 | 항목명 | 보류 | 왜 애매한지 | 나중에 어떤 조건에서 판단할지 | 관련 파일/문서 |
```

브랜치 구현 기록에는 아래 문장을 남긴다.

```text
이 항목은 현재 브랜치에서 제거하지 않고 `docs/14_deferred_cleanup/01_보류_정리_목록.md`로 이관했다.
```
